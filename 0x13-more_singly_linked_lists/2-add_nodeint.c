#include <stdio.h>
#include <stdlib.h>
#include "lists.h"

/**
  *add_node - adds new node at the head
  *@h: pointer to head
  *@n: insert new node
  *return: ptr or NULL
  */

listint_t *add_nodeint(listint_t **head, const int n)
{
	listint_t *new;

	if (ptr == NULL)
		_putchar("%c", o);
	new_node = ptr;
	ptr = ptr->next;
		new_node->data = val;
		new_node->next = head;
		head = new_node;

			return (new);
}
